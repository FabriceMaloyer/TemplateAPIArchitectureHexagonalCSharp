Server Name: You should use the IP address of the host machine (where Docker is running) or localhost if you are connecting from the same machine.

Authentication: You can use SQL Server Authentication with the following credentials:

Username: sa (default administrator username)
Password: yourStrongPassword (as specified in your Docker Compose file)


Command is : docker compose up -d